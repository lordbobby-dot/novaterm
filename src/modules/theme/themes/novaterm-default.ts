import type { Theme } from "../types";

export const novatermDefault: Theme = {
  id: "novaterm-default",
  name: "NovaTerm Default",
  description: "The default NovaTerm look — clean glass over neutral surfaces.",
  editorTheme: { dark: "atomone", light: "atomone" },
  variants: {
    light: {},
    dark: {},
  },
};
